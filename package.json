{
  "homepage": "http://Ironious4.github.io/Challenge-2-Bot-Battlr",
  "name": "react-hooks-bot-battlr-code-challenge",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "^4.0.3",
    "semantic-ui-css": "^2.4.1",
    "web-vitals": "^0.2.4"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "json-server": "^0.15.1"
  },
  "scripts": {
    "start": "set PORT=8001 && react-scripts start",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "react-scripts build",
    "server": "json-server --watch db.json -p 8002"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
